# Install (two steps, in this order):
#   pip install -r requirements-test.txt
#   pip install aiohttp==3.12.15
#
# aioresponses' response builder is not yet updated for aiohttp's newer
# ClientResponse signature (missing "stream_writer" kwarg), so once
# pytest-homeassistant-custom-component pulls in homeassistant (and its
# strict aiohttp==3.14.1 pin), aiohttp must be downgraded afterward to a
# version that still satisfies homeassistant's actual runtime usage while
# staying compatible with aioresponses. pip will print a benign dependency
# warning for this second step; it is expected and safe to ignore.
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-homeassistant-custom-component==0.13.346
aioresponses==0.7.9
